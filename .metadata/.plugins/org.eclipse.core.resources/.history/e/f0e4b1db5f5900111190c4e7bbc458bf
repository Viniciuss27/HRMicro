spring:
  application:
    name: hr-worker
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: test

management:
  endpoints:
    web:
      exposure:
        include: "*"